<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DigiVibe Events | Luxury White & Pink</title>
    <style>
        :root {
            --primary-pink: #db2777; 
            --soft-pink: #fdf2f8;    
            --white: #ffffff;
            --text-dark: #1e293b;
            --transition: all 0.3s ease;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', Arial, sans-serif; }
        html { scroll-behavior: smooth; }
        body { line-height: 1.6; color: var(--text-dark); background-color: var(--white); }

        /* Navigation */
        header {
            background: var(--white);
            padding: 1rem 5%;
            position: sticky;
            top: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 15px rgba(219,39,119,0.1);
        }
        .logo { font-size: 1.6rem; font-weight: 800; text-transform: uppercase; }
        .logo span { color: var(--primary-pink); }
        
        .nav-toggle { display: none; background: none; border: none; font-size: 1.8rem; cursor: pointer; color: var(--primary-pink); }
        
        nav ul { display: flex; list-style: none; gap: 10px; }
        nav a { color: var(--text-dark); text-decoration: none; padding: 0.5rem 1rem; font-weight: 600; transition: var(--transition); }
        nav a:hover { color: var(--primary-pink); }

        /* Hero Slider */
        #home { height: 90vh; position: relative; overflow: hidden; display: flex; align-items: center; justify-content: center; text-align: center; color: white; }
        .slider { position: absolute; inset: 0; z-index: -1; }
        .slide { position: absolute; inset: 0; background-size: cover; background-position: center; opacity: 0; transition: opacity 1s ease; }
        .slide.active { opacity: 1; }
        .slide::after { content: ''; position: absolute; inset: 0; background: rgba(0,0,0,0.4); }

        /* Sections */
        section { padding: 80px 5%; }
        .container { max-width: 1200px; margin: 0 auto; }
        .section-title { text-align: center; font-size: 2.5rem; margin-bottom: 3rem; }
        .section-title span { color: var(--primary-pink); }

        /* Services Grid */
        .services-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; }
        .service-card { 
            background: var(--white); 
            padding: 40px; 
            border-radius: 20px; 
            text-align: center; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.05); 
            border: 1px solid #eee; 
            text-decoration: none; 
            color: inherit; 
            display: block; 
            transition: 0.3s; 
        }
        .service-card:hover { transform: translateY(-10px); border-color: var(--primary-pink); }
        .service-card h3 { color: var(--primary-pink); margin-bottom: 15px; }

        /* Stats Section */
        .stats-container { background: var(--soft-pink); display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; padding: 50px 5%; text-align: center; margin: 40px 0; border-radius: 20px; }
        .stat-item h2 { font-size: 2.5rem; color: var(--primary-pink); }

        /* Booking Form */
        #booking { background: #fff; padding: 60px 5%; }
        .booking-form { max-width: 800px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 20px; background: var(--soft-pink); padding: 40px; border-radius: 20px; }
        .form-group { display: flex; flex-direction: column; }
        .form-group label { font-weight: 600; margin-bottom: 5px; }
        .form-group input, .form-group select, .form-group textarea { padding: 12px; border: none; border-radius: 8px; }
        .btn { padding: 15px 30px; background: var(--primary-pink); color: white; border: none; border-radius: 50px; font-weight: 700; cursor: pointer; transition: 0.3s; grid-column: 1 / -1; text-align: center; text-decoration: none; display: inline-block; }
        .btn:hover { background: #be185d; transform: scale(1.02); }

        /* Contact Section */
        .contact-row { display: grid; grid-template-columns: 1fr 1fr; gap: 50px; }
        .feedback-form input, .feedback-form textarea { width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid #ddd; border-radius: 8px; }

        /* Responsive */
        @media (max-width: 768px) {
            .nav-toggle { display: block; }
            nav ul { display: none; flex-direction: column; position: absolute; top: 100%; left: 0; width: 100%; background: white; padding: 20px; box-shadow: 0 10px 10px rgba(0,0,0,0.1); }
            nav ul.show { display: flex; }
            .booking-form, .contact-row { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">DigiVibe <span>Events</span></div>
        <button class="nav-toggle" id="menuBtn">☰</button>
        <nav>
            <ul id="navLinks">
                <li><a href="#home">Home</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#services">Services</a></li>
                <li><a href="#booking">Book Now</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>

    <section id="home">
        <div class="slider">
            <div class="slide active" style="background-image: url('https://images.unsplash.com/photo-1583939003579-730e3918a45a?q=80&w=1920')"></div>
            <div class="slide" style="background-image: url('https://images.unsplash.com/photo-1530103043960-ef38714abb15?q=80&w=1920')"></div>
            <div class="slide" style="background-image: url('https://images.unsplash.com/photo-1511578314322-379afb476865?q=80&w=1920')"></div>
        </div>
        <div style="z-index: 10;">
            <h1>Celebrate In <span>Style</span></h1>
            <p>Pakistan's Premium Event Management</p>
            <a href="#booking" class="btn" style="margin-top: 20px;">Get Started</a>
        </div>
    </section>

    <section id="about" class="container">
        <h2 class="section-title">About <span>Us</span></h2>
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 40px; align-items: center;">
            <p>DigiVibe Events is Lahore's premier event management system. We specialize in creating high-end, stress-free experiences. Whether it's a traditional Pakistani wedding or a modern corporate launch, we bring perfection to every detail.</p>
            <div style="background: var(--soft-pink); padding: 30px; border-radius: 15px; border-left: 5px solid var(--primary-pink);">
                <h4>Our Mission</h4>
                <p>To provide high-end, stress-free event planning for weddings, birthdays, and corporate gatherings.</p>
            </div>
        </div>
    </section>

    <section id="services" class="container">
        <h2 class="section-title">Our <span>Services</span></h2>
        <div class="services-grid">
            <a href="services/wedding.html" class="service-card">
                <h3>Wedding Planning</h3>
                <p>Traditional Barat, Mehndi & Valima setups.</p>
                <small style="color:var(--primary-pink)">View Details →</small>
            </a>
            <a href="services/birthday.html" class="service-card">
                <h3>Birthday Events</h3>
                <p>Themed decorations for kids and adults.</p>
                <small style="color:var(--primary-pink)">View Details →</small>
            </a>
            <a href="services/corporate.html" class="service-card">
                <h3>Corporate Events</h3>
                <p>Professional summits, launches and meetings.</p>
                <small style="color:var(--primary-pink)">View Details →</small>
            </a>
        </div>

        <div class="stats-container">
            <div class="stat-item"><h2>420+</h2><p>Events Organized</p></div>
            <div class="stat-item"><h2>380+</h2><p>Happy Clients</p></div>
            <div class="stat-item"><h2>75+</h2><p>Trusted Vendors</p></div>
            <div class="stat-item"><h2>15+</h2><p>Awards Won</p></div>
        </div>
    </section>

    <section id="booking">
        <h2 class="section-title">Book Your <span>Event</span></h2>
        <form class="booking-form">
            <div class="form-group"><label>Event Type</label>
                <select required>
                    <option value="">Select Event</option>
                    <option>Wedding Ceremony</option>
                    <option>Birthday Party</option>
                    <option>Corporate Meeting</option>
                </select>
            </div>
            <div class="form-group"><label>Date</label><input type="date" required></div>
            <div class="form-group"><label>Guests</label><input type="number" placeholder="No. of Guests" required></div>
            <div class="form-group"><label>Budget (PKR)</label><input type="text" placeholder="e.g. 5 Lakh" required></div>
            <div class="form-group" style="grid-column: 1 / -1;"><label>Special Requirements</label>
                <textarea rows="4" placeholder="Any specific theme or details?"></textarea>
            </div>
            <button type="submit" class="btn">Send Request</button>
        </form>
    </section>

    <section id="contact" class="container">
        <h2 class="section-title">Get In <span>Touch</span></h2>
        <div class="contact-row">
            <div>
                <h3>Contact Info</h3><br>
                <p><strong>📍 Address:</strong> UMT, Johar Town, Lahore.</p>
                <p><strong>📞 Phone:</strong> +9322-6148189</p>
                <p><strong>✉️ Email:</strong> s2024463004@umt.edu.com</p>
                <br>
                <div style="background: var(--primary-pink); color: white; padding: 25px; border-radius: 15px;">
                    <p>"The pink theme was absolutely breathtaking. Thank you DigiVibe!"</p>
                    <p align="right"><strong>- Happy Client</strong></p>
                </div>
            </div>
            <div class="feedback-form">
                <h3>Feedback Form</h3><br>
                <form>
                    <input type="text" placeholder="Your Name" required>
                    <input type="email" placeholder="Your Email" required>
                    <textarea rows="4" placeholder="Your Message/Feedback"></textarea>
                    <button type="submit" class="btn">Submit Feedback</button>
                </form>
            </div>
        </div>
    </section>

    <footer style="text-align: center; padding: 40px; background: #1e293b; color: white;">
        <p>&copy; 2026 DigiVibe Events | Luxury Event Management</p>
    </footer>

    <script>
        // Hamburger Menu Toggle
        const menuBtn = document.getElementById('menuBtn');
        const navLinks = document.getElementById('navLinks');
        menuBtn.addEventListener('click', () => navLinks.classList.toggle('show'));

        // Slider Logic
        const slides = document.querySelectorAll('.slide');
        let currentSlide = 0;
        setInterval(() => {
            slides[currentSlide].classList.remove('active');
            currentSlide = (currentSlide + 1) % slides.length;
            slides[currentSlide].classList.add('active');
        }, 4000);

        // Form Alert
        document.querySelectorAll('form').forEach(form => {
            form.onsubmit = (e) => {
                e.preventDefault();
                alert('Request Sent Successfully! Our team will contact you.');
                form.reset();
            };
        });
    </script>
</body>
</html>